코틀린에서는 `n`개의 원소를 갖는 [Collections](Collections.md)에 대해서 구조 분해할 수 있다. 

```kotlin
val (number, name) = 1 to "one"
for ((index, element) in collection.withIndex()) {
    println("$index: $element")
}
```
