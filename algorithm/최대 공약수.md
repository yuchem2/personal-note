## 재귀

```python
def gcd(a, b):
	if b == 0:
		return a
	return gcd(b, a % b)
```

## 반복

```python
def gcd(a, b):
	while b != 0:
		a, b = b, a % b
	return a
```

## 활용(최소공배수)

```python
def lcm(a, b):
	return (a * b) // gcd(a, b)
```
