> 6주차 스터디: 2026.05.12
> 10장 학습 후 느낀점 공유 & 질문

- 고차함수
- 람다
- 인라인 함수
- 비로컬return과 로컬return
## 질문 리뷰
- JVM은 바이트코드를 실제 기계어 코드로 번역하는 과정에서 일어나고, ... 해당 문락에 대한 이해가 잘 되지 않아서 해당 내용에 대한 설명을 다시 해주실 수 있나요?
    - JVM 인라인은 중복되지 않는데, 코틀린의 인라이닝은 중복된다. = 내용
    - JVM 인라이닝은 같은 함수 호출이면 기계어로 한번만 번역하고, 해당 부분을 참조하게끔 만들지만, 코틀린의 인라이닝은 함수 호출부분을 함수 본문으로 치환하기 때문에 이후 기계어로 번역될 때 똑같은 코드가 아니라 같은 코드로 보여져 JVM이 JIT 등으로 해결하지 못한다는 내용.
- 람다를 만들 때 => 고차함수를 만들 때 인라인으로 만들지 않는 사례가 있나요?
