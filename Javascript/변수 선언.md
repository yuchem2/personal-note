자바스크립트에서는 변수 선언을 위해 [var](var.md), [let](Javascript/let.md), [const](const.md)를 이용해 변수를 선언한다. 각 키워드에 따라 자바스크립트 엔진은 다른 규칙, 다른 방법으로 변수를 선언한다. 하지만, 키워드가 달라도 항상 변수 이름과 값 모두 [실행 컨텍스트](실행%20컨텍스트.md) 내 키/값 형식의 객체로 등록되어 관리된다. 
